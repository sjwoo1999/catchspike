import 'package:flutter/material.dart';

class CommunityContent extends StatelessWidget {
  CommunityContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('커뮤니티 화면'),
    );
  }
}
